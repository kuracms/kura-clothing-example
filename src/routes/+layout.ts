// Run rendering on the server (default) - keeps the kura token off the client
// and ensures fresh data on each request from Cloudflare's edge.
export const prerender = false;
export const ssr = true;
